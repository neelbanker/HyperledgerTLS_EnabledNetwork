# Hyperledger Fabric TLS Enabled Network
*    Network Setup
        1. Go to network folder and run 1.startNetwork script and
        to create crypto materials uncomment 5th Line in the script.

        2. After generating set the key in docker-compose-base.yaml 
        file in ca container form the ca folder of org1 "_sk" file.

        3. Run the 1.startNetwork script with commenting the 5th line